[pytest]
# Use a project-local cache directory to avoid permission errors when pytest
# attempts to write to /home/.pytest_cache in restricted environments.
cache_dir = .pytest_cache

# Keep default test discovery and any plugins used by the project
addopts = -q
